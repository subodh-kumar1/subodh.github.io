import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: { 
            main: '#8deb31'
        },
        secondary: {
            main: "#8f31eb",
        }
    }
});

export default theme;